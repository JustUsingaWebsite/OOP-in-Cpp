#include "../header/game.hpp"

void Game::setGameName(const string& name, int id){
    //check the name string so it's correct with
    //ur standards to prevent injection

    //then jsut change the name feild.
}

string Game::getGameName(int game_id) const{
    // then return the name, simple
}